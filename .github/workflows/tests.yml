name: tests

on:
  workflow_call:
  push:
    branches: [ "dev", "development" ]

jobs:
  run_tests:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run Tests
        run: echo "No tests yet"
      